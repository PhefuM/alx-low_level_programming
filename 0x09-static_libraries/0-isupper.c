#include "main.h"
/**
 * _isupper - function checks if character is uppercase
 * @c: tested character
 * Return: 1 if uppercase, 0 if not
 */

int _isupper(int c);
{
if  ((c >= 'A') && (c <= 'Z'))
	return (1);

return (0);
}
